# GitLab personal access token (scopes: api). Either env var name works;
# TRACKER_GITLAB_TOKEN is checked first, then GITLAB_PERSONAL_ACCESS_TOKEN.
# An exported shell variable also works — this file is optional.
TRACKER_GITLAB_TOKEN=your-gitlab-token-here
